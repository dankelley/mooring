#' @param width numeric value for the width (in m)
#' of the element as viewed from above.
#' This is multiplied by `height` to compute the
#' area of the element, as part of the drag computation.
#' Note that `width` is ignored if `model` is recognized.

