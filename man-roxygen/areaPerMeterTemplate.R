#' @param areaPerMeter numeric value indicating the frontal area per meter
#' of length, expressed as m^2. (This is equivalent to the width, or diameter, of
#' the wire.)
#' Note that `areaPerMeter` is ignored if `model` is recognized.

