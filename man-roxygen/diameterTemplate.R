#' @param diameter numeric value for the diameter (in m)
#' of the element as viewed from a horizontal direction.
#' This is used to compute area, for drag calculations, as
#' pi*(diameter/2)^2.
#' Note that `diameter` is ignored if `model` is recognized.
